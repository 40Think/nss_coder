# search_dependencies.py - Визуализация

## Mermaid диаграмма
См. `search_dependencies.mmd` - техническая диаграмма с полными потоками данных.

## Метафорическая визуализация
См. `search_dependencies_metaphor.png` - художественное представление системы.

### Концепция: Мистическая Библиотека Зависимостей

Система представлена как древняя магическая библиотека с тремя механизмами оптимизации:

#### 1. **LRU Cache Chamber** (Левая сторона)
- **Метафора**: Кристаллическое хранилище памяти
- **Визуализация**: 100 плавающих свитков, яркость = свежесть
- **Элементы**:
  - Золотые свитки = cache hits
  - Серебряные свитки = загрузка с диска
  - Магические песочные часы = статистика hit/miss
- **Реальность**: 10x+ speedup на повторных запросах

#### 2. **Inverted Index Codex** (Центр)
- **Метафора**: Массивная вращающаяся книга с мгновенным доступом
- **Визуализация**: Страницы с именами модулей и светящимися нитями
- **Элементы**:
  - Золотой символ "O(1)" над книгой
  - Зеленые нити = прямые импорты
  - Синие нити = вызовы функций
- **Реальность**: O(1) lookup вместо O(N) сканирования

#### 3. **NetworkX Graph Observatory** (Правая сторона)
- **Метафора**: 3D созвездие узлов и связей
- **Визуализация**: 910 светящихся узлов, 2977 рёбер
- **Элементы**:
  - BFS волны = транзитивные зависимости
  - Детектор циклов = сканирующая сфера
  - Зелёный свет "No cycles found"
- **Реальность**: DAG граф для сложных запросов

### Потоки данных

**Входы (снизу, 5 потоков)**:
1. 🔵 Синий - Code dependencies (imports, calls)
2. 🟡 Жёлтый - Config files (пусто, self-contained)
3. 🟢 Зелёный - Data files (JSON dependency maps)
4. 🔴 Красный - External APIs (нет, offline)
5. 🟣 Фиолетовый - User queries (CLI)

**Выходы (сверху, 3 портала)**:
1. 📝 Text format (с emoji)
2. 🎨 Mermaid diagram
3. 📋 JSON format

### Метрики производительности
- **10-100x speedup** (LRU Cache)
- **O(1) lookup** (Inverted Index)
- **910 nodes, 2977 edges** (NetworkX Graph)

---

## Файлы

| Файл | Тип | Описание |
|------|-----|----------|
| `search_dependencies.py` | Code | Основная реализация |
| `search_dependencies.mmd` | Diagram | Техническая Mermaid диаграмма |
| `search_dependencies_metaphor.png` | Image | Метафорическая визуализация |
| `search_dependencies.pseudo.md` | Docs | Псевдокод |

---

## Использование визуализаций

**Для разработчиков**: `search_dependencies.mmd` - точные потоки данных  
**Для презентаций**: `search_dependencies_metaphor.png` - концептуальное понимание  
**Для документации**: оба файла дополняют друг друга
